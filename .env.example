# Copy to .env — the only secret the sous-chef needs.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (same names as config.yaml, upper-cased with SOUS_CHEF_ prefix)
# SOUS_CHEF_ROBOT_HOST=192.168.1.42
# SOUS_CHEF_WHISPER_MODEL=medium
# SOUS_CHEF_REQUIRE_NAME=false
